import React from 'react';

const messageBoard = (props) => {

    // let session = JSON.parse(localStorage.getItem('session'));
    // let msg = null;
    // if (session.error != null && session.error !== '') {
    //     msg = (<h3 className="text-danger">{session.error}</h3>)
    // }
    // else if (session.success != null && session.success !== '') {
    //     msg = (<h3 className="text-success">{session.success}</h3>)
    // }
    // return msg;
    return null;    
}

export default messageBoard;